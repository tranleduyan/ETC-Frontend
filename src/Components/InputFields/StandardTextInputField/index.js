/* This is ES6 module syntax to export the default export
from the './StandardTextInputField' module (StandardTextInputField.js file).
The 'default' keyword is used because it's the default export
from the './StandardTextInputField' module. */
export { default } from "./StandardTextInputField";
