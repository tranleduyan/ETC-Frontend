/* This is ES6 module syntax to export the default export
from the './DatePickerInputField' module (DatePickerInputField.js file).
The 'default' keyword is used because it's the default export
from the './DatePickerInputField' module. */
export { default } from "./DatePickerInputField";
