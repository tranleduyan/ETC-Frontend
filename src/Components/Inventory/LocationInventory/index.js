/* This is ES6 module syntax to export the default export
from the './LocationInventory' module (LocationInventory.js file).
The 'default' keyword is used because it's the default export
from the './LocationInventory' module. */
export { default } from "./LocationInventory";
