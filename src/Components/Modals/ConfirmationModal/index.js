/* This is ES6 module syntax to export the default export
from the './ConfirmationModal' module (ConfirmationModal.js file).
The 'default' keyword is used because it's the default export
from the './ConfirmationModal' module. */
export { default } from "./ConfirmationModal";
