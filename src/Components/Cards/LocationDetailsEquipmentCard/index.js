/* This is ES6 module syntax to export the default export
from the './LocationDetailsEquipmentCard' module (LocationDetailsEquipmentCard.js file).
The 'default' keyword is used because it's the default export
from the './LocationDetailsEquipmentCard' module. */
export { default } from "./LocationDetailsEquipmentCard";
