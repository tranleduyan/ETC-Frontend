/* This is ES6 module syntax to export the default export
from the './EquipmentDetailCard' module (EquipmentDetailCard.js file).
The 'default' keyword is used because it's the default export
from the './EquipmentDetailCard' module. */
export { default } from './EquipmentDetailCard';
