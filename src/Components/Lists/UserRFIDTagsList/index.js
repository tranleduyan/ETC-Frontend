/* This is ES6 module syntax to export the default export
from the './UserRFIDTagsList' module (UserRFIDTagsList.js file).
The 'default' keyword is used because it's the default export
from the './UserRFIDTagsList' module. */
export { default } from "./UserRFIDTagsList";
