/* This is ES6 module syntax to export the default export
from the './EquipmentRFIDTagsList' module (EquipmentRFIDTagsList.js file).
The 'default' keyword is used because it's the default export
from the './EquipmentRFIDTagsList' module. */
export { default } from "./EquipmentRFIDTagsList";
