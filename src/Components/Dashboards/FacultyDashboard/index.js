/* This is ES6 module syntax to export the default export
from the './FacultyDashboard' module (FacultyDashboard.js file).
The 'default' keyword is used because it's the default export
from the './FacultyDashboard' module. */
export { default } from './FacultyDashboard';
