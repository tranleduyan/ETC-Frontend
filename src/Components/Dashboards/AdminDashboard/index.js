/* This is ES6 module syntax to export the default export
from the './AdminDashboard' module (AdminDashboard.js file).
The 'default' keyword is used because it's the default export
from the './AdminDashboard' module. */
export { default } from './AdminDashboard';
