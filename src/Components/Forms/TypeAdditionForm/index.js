/* This is ES6 module syntax to export the default export
from the './TypeAdditionForm' module (TypeAdditionForm.js file).
The 'default' keyword is used because it's the default export
from the './TypeAdditionForm' module. */
export { default } from './TypeAdditionForm';
