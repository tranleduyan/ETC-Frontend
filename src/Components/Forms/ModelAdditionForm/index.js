/* This is ES6 module syntax to export the default export
from the './ModelAdditionForm' module (ModelAdditionForm.js file).
The 'default' keyword is used because it's the default export
from the './ModelAdditionForm' module. */
export { default } from './ModelAdditionForm';
