/* This is ES6 module syntax to export the default export
from the './EquipmentAdditionForm' module (EquipmentAdditionForm.js file).
The 'default' keyword is used because it's the default export
from the './EquipmentAdditionForm' module. */
export { default } from './EquipmentAdditionForm';
